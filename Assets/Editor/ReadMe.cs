/*
 * Scripts inside Editor folder will be ignored by Unity when building the final project,
 * so certain build errors related to the scripts can be avoid.
 * Note: put CoordinateLabeler inside to make it still available to use Editor.snapSettings in code while circumventing build errors.
 */
